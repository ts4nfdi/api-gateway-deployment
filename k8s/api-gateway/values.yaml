ingress:
  dns:
  redirectDns:
  enableSSL:
  certIssuer:

images:
  backend:
  frontend:

gateway:
  admin_password: changeme
  jwt_secret: changeme

service:
  type: ClusterIP
  port: 8443

postgres:
  storage: 1Gi
  database: gateway
  username: admin
  password: changeme
